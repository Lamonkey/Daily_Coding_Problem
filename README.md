# Daily_Coding_Problem

## 142
You're given a string consisting solely of `(`, `)`, and `*`. `*` can represent either a `(`, `)`, or an empty string. Determine whether the parentheses are balanced. For example, `(()*` and `(*)` are balanced. `)*(` is not balanced.

## two sum
Use hash table to solve two sum problem, a more memory efficent solution could be achieved by using 
array along, but only work for number larger than zero
last modified 1/3/2020
